# 04 — Layer Palette

Swap visual themes by providing different Layers.

## Learn

- `Context.Service`
- `Layer.succeed`
- `Effect.provide`

## TODO

1. `yield* ThemeService` and write `service.theme` into the Ref.
2. Provide `SolarLive` or `NocturneLive` based on `useNocturne`.

Click the canvas to toggle. Colors should change with the layer.
