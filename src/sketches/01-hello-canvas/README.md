# 01 — Hello Canvas

Draw a circle using values produced by Effect.

## Learn

- `Effect.succeed`
- `Effect.sync`
- `Effect.runSync`

## TODO

1. In `setup`, compute the canvas center with `Effect.sync`.
2. Pick a radius with `Effect.succeed(48)`.
3. Run both with `Effect.runSync` and store into `x`, `y`, `radius`.

When done: orange circle sits in the middle of the canvas.
